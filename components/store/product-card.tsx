import Image from"next/image";import Link from"next/link";import type{Product}from"@/lib/products";import{formatPrice,hasVariablePrices}from"@/lib/products";export function ProductCard({product}:{product:Product}){return <article className="product-card"><Link href={`/product/${product.slug}`} className="product-image-wrap"><Image src={product.images[0]} alt={product.name} fill sizes="(max-width:639px) 50vw,(max-width:1023px) 33vw,25vw" className="product-image"/>{(product.newArrival||product.bestSeller)&&<span className="product-badge">{product.newArrival?"New":"Bestseller"}</span>}</Link><div className="product-copy"><p>{product.category}</p><Link href={`/product/${product.slug}`}><h3>{product.name}</h3></Link><strong>{hasVariablePrices(product)?"From ":""}{formatPrice(product.sellingPrice)}</strong><span className="product-cod">Cash on Delivery available</span><span>{product.colour} · {product.material}</span></div></article>}
