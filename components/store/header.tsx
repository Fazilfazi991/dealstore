"use client";
import Link from"next/link";import{Menu,Search,ShoppingBag,X}from"lucide-react";import{useState}from"react";import{useCart}from"./cart-provider";
const links=[["New Arrivals","/shop?sort=newest"],["Dresses","/shop?category=Dresses"],["Kurtis","/shop?category=Kurtis"],["Kurta Sets","/shop?category=Kurta+Sets"],["Occasion","/shop?occasion=Festive+wear"],["Best Sellers","/shop?sort=best"]];
export function Header(){const[open,setOpen]=useState(false);const{count}=useCart();return <><div className="announcement">Free delivery across India · Cash on Delivery available</div><header className="site-header"><div className="header-inner"><button className="icon-button mobile-only" onClick={()=>setOpen(true)} aria-label="Open menu"><Menu/></button><Link href="/" className="brand" aria-label="Dealstore home">deal<span>store</span></Link><nav className="desktop-nav" aria-label="Main navigation">{links.map(([l,h])=><Link key={l} href={h}>{l}</Link>)}</nav><div className="header-actions"><Link className="icon-button" href="/shop" aria-label="Search products"><Search/></Link><Link className="cart-link" href="/cart" aria-label={`Shopping bag with ${count} items`}><ShoppingBag/><span>Bag</span>{count>0&&<b>{count}</b>}</Link></div></div></header>{open&&<div className="menu-backdrop" onClick={()=>setOpen(false)}><nav className="mobile-menu" aria-label="Mobile navigation" onClick={e=>e.stopPropagation()}><div className="mobile-menu-top"><span className="brand">deal<span>store</span></span><button className="icon-button" onClick={()=>setOpen(false)} aria-label="Close menu"><X/></button></div>{links.map(([l,h])=><Link key={l} href={h} onClick={()=>setOpen(false)}>{l}</Link>)}</nav></div>}</>}
