import type{Metadata}from"next";import{cookies}from"next/headers";import{OrderSuccess}from"@/components/store/order-success";import{getOrder}from"@/lib/commerce/repository.server";export const metadata:Metadata={title:"Order confirmed",robots:{index:false,follow:false},referrer:"no-referrer"};export default async function Page({searchParams}:{searchParams:Promise<{order?:string}>}){const{order}=await searchParams;const value=(await cookies()).get("dealstore_order_access")?.value||"";const separator=value.indexOf(".");const cookieOrder=value.slice(0,separator);const token=value.slice(separator+1);const receipt=order&&order===cookieOrder&&token?await getOrder(order,token):null;return <OrderSuccess order={order||"Pending"} receipt={receipt}/>}
