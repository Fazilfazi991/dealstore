import type{Metadata}from"next";import{CheckoutForm}from"@/components/store/checkout-form";export const metadata:Metadata={title:"Checkout",robots:{index:false,follow:false}};export default function Page(){return <main className="page-shell"><CheckoutForm onlinePaymentsAvailable={Boolean(process.env.STRIPE_SECRET_KEY&&process.env.STRIPE_WEBHOOK_SECRET)}/></main>}
