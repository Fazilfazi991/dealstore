import type{Metadata}from"next";import Image from"next/image";import Link from"next/link";import{Banknote,PackageCheck}from"lucide-react";import{notFound}from"next/navigation";import{ProductPurchase}from"@/components/store/product-purchase";import{findProduct,formatPrice,products}from"@/lib/products";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=findProduct(slug);if(!p)return{};return{title:p.name,description:p.description,alternates:{canonical:`/product/${p.slug}`},openGraph:{title:p.name,description:p.description,images:[{url:p.images[0],alt:p.name}]},twitter:{card:"summary_large_image",title:p.name,description:p.description,images:[p.images[0]]}}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=findProduct(slug);if(!p)notFound();const jsonLd={"@context":"https://schema.org","@type":"Product",name:p.name,image:p.images,description:p.description,sku:p.id,brand:{"@type":"Brand",name:"Dealstore"},offers:{"@type":"Offer",priceCurrency:"INR",price:p.sellingPrice,availability:"https://schema.org/InStock",url:`https://dealstore.example/product/${p.slug}`}};return <main className="page-shell product-page"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><nav className="breadcrumbs" aria-label="Breadcrumb"><Link href="/">Home</Link><span>/</span><Link href="/shop">Shop</Link><span>/</span><span>{p.name}</span></nav><div className="pdp-layout"><div className="gallery">{p.images.slice(0,5).map((image,index)=><div className="gallery-image" key={image}><Image src={image} alt={index===0?p.name:`${p.name}, view ${index+1}`} fill priority={index===0} sizes="(max-width:767px) 92vw,42vw"/></div>)}</div><section className="pdp-info"><p className="pdp-category">{p.category} · {p.occasion}</p><h1>{p.name}</h1><p className="pdp-price">{formatPrice(p.sellingPrice)}</p><div className="pdp-trust" aria-label="Purchase benefits"><span><Banknote/>Cash on Delivery available</span><span><PackageCheck/>FREE delivery across India</span></div><p className="pdp-description">{p.description}</p><ProductPurchase product={p}/><dl className="product-details"><div><dt>Material</dt><dd>{p.material}</dd></div><div><dt>Colour</dt><dd>{p.colour}</dd></div><div><dt>Available sizes</dt><dd>{p.sizes.join(", ")}</dd></div><div><dt>Product code</dt><dd>{p.id}</dd></div></dl><div className="policy-note"><strong>Simple delivery, clear total</strong><p>Standard delivery is free. Delivery timing and returns eligibility will be shown when these policies are connected.</p></div></section></div></main>}
